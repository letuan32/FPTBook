@model WebMVC.ViewModels.Books.Responses.BookIndexVm

@{
    ViewBag.Title = "Book management";

}
<div class="container">
    @{
        await Html.RenderPartialAsync("Book/_BookIndexPartial", Model);
    }
</div>